name: First workflow

on: workflow_dispatch #can manually triggers the workflow

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello world"
      - name: Print Goodbye
        run: echo "Done - bye!"
      
